//go:build ignore
// +build ignore

// Removed (B204 cleanup). The original was a one-off
// debug script for the B203.1 StringArray parser; the
// test coverage in internal/db/array_b203_test.go is
// the durable form. This file is a placeholder so the
// next git push doesn't carry the old build artifact.

package main
