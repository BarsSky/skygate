# Slice B200–B262 (68 run_check entries, 66 scripts)

Scope: every `run_check`/`run_check_slow` registration in `scripts/verify_pre_deploy.sh`
whose ID is B200–B262. 68 entries, 66 distinct scripts
(`check_b203.sh` serves B203 **and** B203.1; `check_b202_5.sh` is registered twice,
at `scripts/verify_pre_deploy.sh:3522` and `:4251`).
Style is per **script** (scripts are shared). Legend:
`behavioural` = executes product code / real DB / real server and asserts an outcome;
`structural` = source-text / symbol / file / template / i18n assertions, plus compile-only
(`go build`/`go vet`/staticcheck) checks;
`mixed` = greps plus `go test` over the script's own feature (the executed half is
unit-level, never the live invariant);
`live-only` = the meaningful assertions need docker/PostgreSQL/headscale/VM and SKIP elsewhere.

Gate mechanics that bound every row (verified): `run_check` at
`scripts/verify_pre_deploy.sh:95` counts **rc == 0 as PASS** (`:190-192`), prints
**no output at all on PASS**, has **no SKIP detection**, and on FAIL prints only the
**first 20 lines** (`:195`). CI runs the same catalogue on a runner with no docker
stack (`.github/workflows/ci.yml:111-132`), where every live check SKIPs and the
SKIP is counted as a PASS.

## Table

| ID | script | style | catches | CANNOT catch |
|----|--------|-------|---------|--------------|
| B243-duplicate-users | `check_b_duplicate_users.sh` | live-only | headscale duplicate user names via live CLI + python3 Counter (`:56-93`) | anything off-VM (SKIP→PASS, `:56-63` + `lib/skip_if_no_docker.sh:34-36`); no tag/ownership comparison |
| B243-reconcile-audit | `check_b_reconcile_audit_writes.sh` | live-only | real `docker restart` + audit_log row/ts delta + `SQLSTATE 42601` grep (`:83-141`) | runs only as root on the host; PASS C unreachable unless PASS A (`:121-143`); `docker logs` failure reads as "0 errors" (`:135`) |
| B243-node-owner-map-orphans | `check_b_node_owner_map_orphans.sh` | live-only | `node_owner_map.headscale_user_id` vs live headscale ids (`:57-82`) | a failed psql query produces PASS A (`:71-79`); the `tag` column is selected (`:64`) but never compared |
| B203 | `check_b203.sh` | mixed | swapdb/watchdog/DBSource symbol greps + `go build/vet` + `go test -run` over `internal/db` + `internal/watchdog` (`:203-220`) | no live pool swap; `TestDBSwap` alternative matches no test (`:215`); no-go → PASS (`:222`); `\*sql.DB`/`sql.ErrConnDone` satisfied by comments (`:75,:110`) |
| B202.5 | `check_b202_5.sh` | mixed | SSHDumpTransport symbols + `go test ./internal/dbmigrate/...` (`:208`) | field/env greps are file-scoped not struct-scoped (`:100-137`); the one round-trip test `t.Skip`s on Windows (`ssh_transport_test.go:146`) |
| B210 | `check_b210.sh` | mixed | DBSource migration greps + build/vet + package tests (`:191-209`) | the four "main.go passes ResettableDB" pairs are unscoped `DB:\s+d,` (`:96-99,125-128,155-158,171-174`); auth scan limited to `service.go` (`:89-93`); no live hot-reload |
| B210.1 | `check_b210_1.sh` | mixed | canonical DBSource/FixedDBSource/DBCurrent greps + package tests (`:130-140`) | the type-alias spelling is text-matched (`:91` — `skygatedb.DBSource` would false-FAIL) |
| B207_fix | `check_b207_fix.sh` | structural | 4 file greps on `clear_test_dsn.sh` + one on `b208_verify.sh` (`:72-101`) | never executes the psql cleanup; `sudo -u postgres` satisfied by a comment (`:84`); "final step" is only presence (`:91`) |
| B211 | `check_b211.sh` | mixed | `skygate init` source pins + `go test` + `init --help` build (`:161-204`) | `:128` awk\|grep -q under pipefail can report PASS on the "state = present" regression; `:144` only proves a `DO $$` block exists; usage string ≠ bootstrap |
| B212 | `check_b212.sh` | mixed | join/DSN pins + `go test` + `join --help` build (`:141-179`) | `:128` satisfied by the *definition* of `substituteDSNTemplate` (`join.go:483`); `:100` by a doc comment (`cluster.go:229`); live handshake never run |
| B213 | `check_b213.sh` | mixed | pgMigrations/`migrate` pins + tests + `--help` (`:137-173`) | `:120` satisfied by the comment at `migration_tracking.go:167`; applied_migrations never read live |
| B214 | `check_b214.sh` | mixed | async/cancel/rollback pins + tests (`:132-159`) | `:91` "is async" satisfied by **any** `go func()` in the file; `:113-120` i18n presence-only; no real run/cancel/rollback |
| B215 | `check_b215.sh` | mixed | cluster_audit helper/action pins + tests (`:139-156`) | `:124-131` i18n presence-only (one-sided key passes); no audit row ever written |
| B216 | `check_b216.sh` | mixed | /admin/cluster query+template pins + `go test -run 'B216\|ExtractDSN\|ClassifyNode\|OnlineThreshold'` (`:204-221`) | **`:96` can never match (row J dead)**; `:156` i18n = occurrence count; no page render |
| B217 | `check_b217.sh` | mixed | drain/approve handler+route+template pins + full `go test ./internal/cluster/...` (`:105-261`) | `:221` satisfied by the state badge (`cluster.html:116`), not the button guard (`:147`); `:105` window ends at `cluster.go:404`; `:199` i18n occurrence count |
| B218 | `check_b218.sh` | mixed | standby-mode pins + `go test -run 'B218\|ParseRolesCSV_Presets\|IsStandbyRole'` (`:176-193`) | **`:168` compares the working tree to HEAD → always green**; `:56` test-name grep; no bootstrap run |
| B219 | `check_b219.sh` | mixed | Patroni switchover pins + `go test -run FailoverDB` (`:140-157`) | `:47` satisfied by comments in `cluster_patroni.go:2-94`; `:117` "doesn't break B218" is a declaration grep; no switchover executed |
| B220 | `check_b220.sh` | mixed | last_failover pins + `go test` (`:157-174`) | **`:125` i18n passes while `catalog_admin.go:897` (RU) is English and `:1927/:1931` (EN) are Russian**; `:141` count-only; `:149` `[skip]` cannot fail |
| B221 | `check_b221.sh` | mixed | audit target pins + `go test -run AppendAuditLogWithTarget` (`:213-230`) | `:112/:133` merged `-A3` window (SIGPIPE → spurious FAIL); `:198` `< 680` also matches; `:213` selects 5 of 6 tests; no row written |
| B222 | `check_b222.sh` | mixed | rolling-upgrade pins + `go test -run 'Rejoin\|Upgrade\|SelfUpgrade\|pollOnce'` (`:232-256`) | **`:232` `pollOnce` is case-dead** (tests are `TestPollOnce_*`) → 4 tests never selected; `:95` duplicates `:84`'s string; no upgrade performed |
| B223 | `check_b223.sh` | mixed | discovery pins + `go test` (`:181-205`) | `:84` satisfied by the `strings.Contains(ip, ":")` idiom, never that IPv4 is returned first; `:154-156` label claims the 5-min default, unverified; `:191/:209` `[skip]` cannot fail |
| B224 | `check_b224.sh` | structural | `.Current()` usage counts + symbol greps + `go build` (`:88-158`) | **`:114` satisfied by the test file's own header comment and no `go test` runs** (4 tests incl. `TestResettableDB_FollowsSwapViaCurrent` never executed); `:93-103` one aggregate `>= 6` over 3 files (monitor alone has 12) |
| B225 | `check_b225.sh` | structural | failover/rollback alert + i18n greps + `go build` (`:56-160`) | **`:56` unanchored sed also matches the Rollback handler**, so A/B pass from its ✅/❌ calls; `:127/:132` test-file comments; 11 tests never run |
| B225.1 | `check_b225_1.sh` | structural | DB-health degraded-alert struct/branch greps (`:49-143`) | `:90/:97/:106` literals only — the baseline early-return and the healthy no-op can be removed/inverted; 7 tests never run; no `go build` and no SKIP line at all |
| B225.2 | `check_b225_2.sh` | structural | dbswap read-failure transition greps (`:48-124`) | `:94/:101` detector names only — swapped calls pass; 8 tests never run; no `go build`, no SKIP |
| B226 | `check_b226.sh` | structural | Prometheus metric/label greps + `go build` (`:88-226`) | `:200` test presence = filename + `B226` + count (8) — the 8 metric tests never compile/run here; `:169/:177/:185` co-occurrence, not pairing; 17 `[ok]` lines precede the late rows |
| B227 | `check_b227.sh` | mixed | TagAlertSink/audit/rate-limit greps + `go test ./internal/nodeownership/... ./internal/metrics/...` (`:236-253`) | nothing touches headscale; `ReconcileTags` unpinned; G2 window misses the metric gate (`:189`) |
| B228 | `check_b228.sh` | structural | derp-dashboard filter/sort greps + `go build` (`:66-142`) | handler→template data hand-off never asserted (`"DERPs": all` would pass) |
| B229 | `check_b229.sh` | mixed | preferred-exit reconciler/logic pins + `go build/vet/test` (`:179-195`) | `applyReconcilerChange` DB write never executed; doc says "default off" while code is default-ON (`:20-22`, `reconciler.go:290-292`) |
| B231 | `check_b231.sh` | mixed | rename-migration wiring pins + `go build/vet/test` (`:246-262`) | UPSERT/`tx.Commit` never executed; `:186-207` is one-file "RU+EN" counting |
| B232 | `check_b232.sh` | mixed | v0.68 migration + driver pins + `go test ./internal/db/...` (`:134-140`) | `:88` matches the test's error message, not the guard; `:105` banks a `t.Skip` stub |
| B233 | `check_b233.sh` | mixed | shape-drift audit pins + `go test -run 'TestMigrations_\|TestShapeDriftAudit'` (`:131-137`) | `:107` "mutation test" is a tautology (`migrations_audit_b233_test.go:450-472`) |
| B234 | *(inline)* `echo "B234 — manual; …"` | none (no-op) | nothing — prints a sentence (`verify_pre_deploy.sh:3743-3744`) | everything: a contract registered as an unconditional `echo` is a permanent PASS |
| B236 | `check_b236.sh` | mixed | advertise-routes/guard symbols + `go build` + admin package tests (`:102-111`) | `--advertise-routes=` and the guards are satisfied by comments/definitions (`:57,:63,:66`); no live `tailscale set` |
| B237 | `check_b237.sh` | mixed | DERP/derpmap pins + `go test -run 'RewriteDerpURLs\|ShortNameFromHostname\|PublicDERPPortFromURL'` (`:247-283`) | `:111` greps the word "idempotent"; config rewrite/atomic write/docker restart never executed |
| B237.7 | `check_b237_7.sh` | mixed | reconciler-live default/opt-out greps + real `go test` (`:129-143`) | `:46-50` satisfied by the comment at `reconciler.go:295`; SIGPIPE `grep -q` at `:129,:142` |
| B237.2 | `check_b237_2.sh` | mixed | DERP public-IP pins + `resolvePublicDERPIP` tests (`:179-192`) | `:89-93` satisfied by the comment at `derp.go:599`; no DNS lookup performed |
| B237.10 | `check_b237_10.sh` | mixed | build-label/manual-steps pins + targeted tests (`:270-297`) | `:176-180` regex matches the regressed form; SIGPIPE `grep -q` at `:270,:283,:296` |
| B237.15 | `check_b237_15.sh` | structural | installer/workflow greps, `bash -n`, PS parse, `go build` (`:420-428`) | no installer is executed; contracts E.1–J.2 are output lines 21-58 → invisible on FAIL |
| B237.16 | `check_b237_16.sh` | structural | cleanup pins + staticcheck + package-wide build/vet/test (`:153-185`) | `:127-132` count never compared; `:54-58` comment filter is a no-op; `:103,:112` staticcheck silent-zero |
| B237.17 | `check_b237_17.sh` | structural | psql-script/idempotency pins (`:105-190`) | `:190-194` ±window swallows ROADMAP TD-10 `**DONE**`; no psql runs |
| B237.18 | `check_b237_18.sh` | mixed | reconcile pins + real reconcile unit tests (`:226-268`) | `:86-90`/`:218-222` duplicate inverted greps pass on a missing file; `:255-259` window swallows TD-9 `**DONE**` |
| B237.19 | `check_b237_19.sh` | mixed | duplicate-alert pins + `buildFormErrorRedirectURL` tests (`:168-190`) | `:98-100` counts echoed as INFO, never asserted; `:159-163` passes if the catalogue is deleted |
| B237.20 | `check_b237_20.sh` | structural | U1000/ST1013/SA1012/S1021 zero-counts + build/package tests (`:46-201`) | five `staticcheck … \|\| true` counts read 0 when staticcheck itself fails (`:54,69,82,83,97`); `:194-201` no rc check → cross-package masking |
| B237.21 | `check_b237_21.sh` | mixed | regapi credential masking pins + `cmd/skygate` tests (`:171-212`) | `:135-143` `! grep maskSecret(...)` is always false (required by `:102-106`) → `bad` unreachable |
| B237.22 | `check_b237_22.sh` | mixed | CDN-group pins + `go test ./internal/feature/exit_rules/...` (`:229-233`) | `:195-199` greps a path with no files; `:229-233` turns a real test FAIL into PASS under pipefail; `:298-317` reports "clean" on pipeline failure |
| B237.23 | `check_b237_23.sh` | mixed | live PG index probe + docker grep + unit runs (`:130-198`) | E.1 hard-FAILs (not SKIPs) when psql/DB is unavailable (`:131-136`) |
| B237.24.1 | `check_b237_24.sh` | structural | release.yml pins + `git ls-remote`/`gh release view` (SKIP-guarded) + `go build/vet` (`:133-223`) | `:67-74` counts comment hits as tag lines; pins the historical v1.5.2 tag; no test runs |
| B235 | `check_b235.sh` | mixed | i18n/derp-health/template-arg pins + `go build` + `go test` (`:181-216`) | `:210-216` `grep -q '^ok'` also matches `ok … [no tests to run]`; `:115` `\.Name` matches anything |
| B202.5 *(duplicate registration)* | `check_b202_5.sh` | mixed | same as above; runs twice per gate | same as above |
| B209 | `check_b209.sh` | mixed | elector Config.Now pins + `go test ./internal/elector/...` (`:186-199`) | never runs `scripts/b209_e2e.sh` (only greps it, `:72-126`); `:116-119` string presence ≠ exit-code contract |
| B208 | `check_b208.sh` | mixed | DBSource migration pins + build/vet/tests (`:190-208`) | `:90-94` `-A3` window can never reach `service.go:103` → permanent PASS; `:143/:148` windows overlap; 20 sites = summary always truncated |
| B207 | `check_b207.sh` | mixed | unified-audit function-body awk pins + tests (`:230-241`) | no real audit_log/cluster_audit row is read; `:166-175` are plain content greps |
| B206 | `check_b206.sh` | mixed | /db/health sampler pins + healthz tests (`:202-216`) | `:172-183` `-A200` swallows the next function (`db_health.go:672`); `:105` satisfied by a comment; `:167` zero-margin window; no PG query runs |
| B205 | `check_b205.sh` | mixed | cluster CLI pins + `cmd/skygate` tests (`:213-224`) | `:103`/`:111` windows hold only comments (`cluster.go:173`, `:295-296`); no CLI executed |
| B204 | `check_b204.sh` | mixed | elector state-machine pins + `go test ./internal/elector/` (`:168-180`) | pure-function tests only; `:130` `-A100` measures function length; no DB transition |
| B203.1 | `check_b203.sh` | mixed | (same script as B203) COALESCE + NULL tests (`:233-247`) | B203.1-specific contracts are inseparable from B203's; second full run per gate |
| B244 | `check_b244.sh` | structural | go.mod/README version pin (C1, `:88-96`) + typo/doubled-path absence pins | absence pins PASS when the pinned file is deleted (`:99-148`); C8/C9 never check `catalog_bot.go` exists (`:156,:170`) |
| B249 | `check_b249.sh` | structural | image-pull symbols + `func Test…` name presence (`:103-119`) | "(RU)" greps accept any occurrence (`:87-92`); `docker pull`→`compose up`→rollback never executed |
| B250 | `check_b250.sh` | structural | ACL pretty-print pins + dark-card CSS greps (`:40-99`) | section C cannot fail (`:91-99`, both arms PASS); B1 window misses the card line (`:58`) |
| B252 *(ACL grouped view)* | `check_b252.sh` | structural | category/parse/template pins (`:51-160`) | `:122-123` "RU+EN" is one-file occurrence counting (dots as regex); `-A10` slice has no margin (`:51-53`) |
| B251 | `check_b251.sh` | structural | strict-equality/hujson/tagOwners symbol greps (`:59-170`) | **the script always exits 0** (`:56-57,:173`) → all 12 contracts can regress invisibly; nothing proves headscale accepts the policy |
| B252 *(DERP cert sync)* | `check_b252_derp_cert_sync.sh` | structural | migration columns/driver/cron/route/template/i18n greps (`:55-192`) | cert expiry/renewal outcome asserted nowhere; awk column block is not dialect-scoped (`:85-95`) |
| B253 | `check_b253_telegram_async.sh` | structural | async-refresh symbol/template/i18n greps (`:39-122`) | the non-blocking guarantee is never asserted (`:39-44`); gofmt alignment pinned literally (`:76,:79`) |
| B256 | `check_b256_other_hs_user_ids_int_literal.sh` | structural | `!= 0` literal pins + `go vet` (`:86-150`) | contract K is a printf + unconditional `ok` (`:161-181`); J prints `ok` when go is missing (`:148`) |
| B258.1 | `check_b258_1_auth_key_missing.sh` | mixed | 3-state auth-key logic pins + `go test -run 'TestTailscaleAuthKeyMissingForStart\|TestAuthKeyMissingMutuallyExclusive'` (`:154-173`) | missing Go prints two `✓` PASSes (`:161,:172`); `:140` substring matches every probe |
| B260 | `check_b260_derp_status_collection.sh` | mixed | DERP URL/host/port/TLS pins + `go vet` + `go test -run TestResolveDERPHostname` (`:200-220`) | `:277` is an unconditional `ok`; deployment gate K passes when the compose file is absent (`:245-249`); no live derper probe |
| B261 | `check_b261_native_self_update.sh` | mixed | applier validation runs (G2/G3), installer checksum rehearsal (P2/P3), release-workflow greps (`:220-638`) | **`skip()` is undefined** (`:597,:637` → exit 127); the download→verify→swap→restart→healthz path is never executed; live sections are prose |

## Dead or near-dead contracts

Gate-level (they make the other findings invisible):

- `scripts/verify_pre_deploy.sh:190-192` — counts `rc == 0` as PASS and prints **no output on PASS** — a check that prints `FAIL` but exits 0 (or prints `SKIP`) is reported as a green PASS with its whole log discarded — parse the `FAIL`/`SKIP` lines in `run_check` and require an explicit per-check exit contract.
- `scripts/verify_pre_deploy.sh:195` — prints only the first 20 lines of a failing check — scripts with a late failure (B237.15, E.1–J.2 are output lines 21-58; B237.22 lines 21-32; B237.10 lines 21-23; B237.18 lines 21-24; B208 at exactly 20 sites loses its summary; B216/226 lose O–Y) show green ✓ lines and no reason — print the `FAIL`/`✗` lines first.
- `scripts/verify_pre_deploy.sh:3743-3744` — B234 is registered as `echo "B234 — manual; verified locally…"` — it cannot fail, so the block has no contract at all — replace with a real script or delete the entry.
- `scripts/verify_pre_deploy.sh:4302` and `:4308` — both register `run_check "B252"` (ACL grouped view / DERP cert sync) — a `FAIL B252` row is ambiguous — rename one ID.

Always-pass / unreachable-assertion contracts:

- `scripts/check_b251.sh:56-57` + `:173` — `bad()` only echoes and the file contains no `exit` — every B251 contract (strict `skygate-host` equality, `findUserForHostname` pin, hujson/unquote) regresses invisibly because the gate sees rc 0 and prints PASS — add `FAIL=0; bad(){ FAIL=$((FAIL+1)); }` and `[ "$FAIL" -eq 0 ]` as the last line.
- `scripts/check_b216.sh:96` — row J, `has "$CLUSTER_GO" "FROM audit_log\\s*\\n.*LIKE 'cluster.%'"` — `has()` is `grep -q -E` (`:23`), which is line-based, so `\n` can never match, and `grep -n 'FROM audit_log' internal/feature/admin/cluster.go` returns **nothing** (only the comments at `:24`/`:378` contain `LIKE 'cluster.%'`) — the row always takes the `ok "pre-B216 audit_log query removed"` branch, so restoring the pre-B216 query is invisible — grep the two tokens separately, or assert `FROM cluster_audit` + the 8-action IN list (`cluster.go:401-404`).
- `scripts/check_b218.sh:168` — row N, `! git diff --name-only HEAD | grep -q "catalog_admin.go"` — compares the working tree with HEAD, so a **committed** catalog change is never seen; on a clean checkout (and when `git` fails, via `pipefail` + `!`) the row is always green, and when it does match it prints `[skip]`, not FAIL — assert RU/EN key parity instead, or delete the row.
- `scripts/check_b224.sh:114` — row I, `[ -f "$TEST_NEW" ] && grep -q "B224" "$TEST_NEW"` — satisfied by the test file's own header comment (`resettable_b224_test.go:1`) — the file may contain zero tests and the row still PASSes, and this script has **no `go test` at all**, so the 4 tests (incl. `TestResettableDB_FollowsSwapViaCurrent`) never run — replace with `go test ./internal/db/ -run ResettableDB`.
- `scripts/check_b225.sh:56` — rows A/B: `sed -n '/^func (s \*Service) PostAdminDatabaseFailover/,/^}$/p'` — the start regex is not anchored, so it also matches `PostAdminDatabaseFailoverRollback` (`internal/db/database.go:636`); the captured block spans both functions (483-601 **and** 636-724), so the rollback handler's own `sendFailoverAlert("✅")` (`:716`) / `("❌")` (`:700`) satisfies A/B even if the failover handler's calls are deleted — anchor with `\(http`.
- `scripts/check_b225.sh:127,:132` — rows J.a/J.b are `[ -f ] && grep -q "B225" <testfile>` — satisfied by the files' header comments, so a test file with zero tests passes and the 11 tests never run — execute them.
- `scripts/check_b225_1.sh:90,:97,:106` — ok→degraded / degraded→ok / "first tick is baseline" are asserted by grepping the literals `verb = "DEGRADED"` (`db_health.go:512`), `verb = "recovered"` (`:515`) and `first := !s.hasFirstSample` (`:491`) — deleting the baseline early-return (`:500`) so every start fires a spurious DEGRADED alert, or inverting the `prev == healthy` no-op (`:504`), leaves all three green; the script has **no `go test`, no `go build` and no SKIP line** — run `TestTransition_*` (`db_health_b225_1_test.go:77-235`).
- `scripts/check_b225_2.sh:94,:101` — "tick() calls the failure/success detector" is asserted only by the method name appearing inside the `tick()` range — swapping the two calls keeps both rows green, and the discriminating state (`consecutiveReadFailures >= ReadFailureThreshold`) is asserted nowhere; no `go test`/`go build` in the script — run the 8 `dbswap_b225_2_test.go` tests.
- `scripts/check_b226.sh:200` — row J claims the 8 metric tests cover monotonicity/escaping/handler — it is only `[ -f ] && grep -q "B226"` + `^func Test >= 8` (exactly today's count) and the script never runs `go test`, so a test file that no longer compiles passes here (only B1's `go test ./...` would notice) — run `go test ./internal/metrics/ -count=1`.
- `scripts/check_b224.sh:93-103` — row G aggregates `grep -c 'DB\.Current()'` over three files with one `>= 6` threshold — `exit_node_monitor.go` alone contributes 12 uses, so `backup.Scheduler` and `nodeownership/auto.go` can revert to the captured-`*sql.DB` anti-pattern while G stays green — per-file minimum, plus a count of the anti-pattern (`\bm\.DB\.` without `.Current()`).
- `scripts/check_b226.sh:169,:177,:185` — rows F/G/H are independent co-occurrence greps, not pairings (`BuildInfoGauge` + `runtime.Version()` anywhere in `main.go`; `s.DB.Current()` + `PingContext` anywhere in `collector.go`; `DBPoolStats()` + `conn.Stats()` anywhere) — a wired-but-wrong exporter passes — assert the call sites inside the same function body.
- `scripts/check_b216.sh:156`, `check_b217.sh:199`, `check_b219.sh:97`, `check_b220.sh:125`, `check_b222.sh:202`, `check_b223.sh:171` — the "RU + EN i18n" rows are `grep -c "\"$key\":" file >= 2` occurrence counts, not language-side parity — **proven live**: `check_b220.sh:125` scores perfectly while `catalog_admin.go:897` (`db.rollback_candidate_ph`, RU map) is English and `:1927`/`:1931` (`db.rollback_help`, `db.rollback_confirm`, EN map) are Russian — awk each map (`/^var ruAdmin/,/^var enAdmin/`) and compare the value language.
- `scripts/check_b214.sh:113,:115,:117,:119` and `check_b215.sh:124,:126,:128,:130` — the "RU + EN, 4 each" rows are presence-only — a key that exists only in the EN map passes.
- `scripts/check_b222.sh:232` — the `go test -run 'Rejoin|Upgrade|SelfUpgrade|pollOnce'` filter's last alternative is case-dead: Go's `-run` is case-sensitive and the tests are `TestPollOnce_*` (`internal/cluster/upgrade_b222_test.go:135,157,173,188`) — the 4 poll-once tests (the `/healthz` build-match logic the block is about) are never selected by this contract — spell it `PollOnce`.
- `scripts/check_b221.sh:213` — the `-run AppendAuditLogWithTarget` filter selects 5 of the file's 6 tests (`TestAppendAuditLog_StillWorks_BackwardCompat` at `audit_log_b221_test.go:100` is not matched) while the description claims 6 — add the missing name.
- `scripts/check_b221.sh:112,:133` — each action is paired with the helper through one merged `grep -B0 -A3 "AppendAuditLogWithTarget"` window over the whole file; under `set -uo pipefail` a `grep -q` early exit can flip a correct pairing into a spurious `[miss]` FAIL (AGENTS trap 9, false-FAIL direction) — capture first, then match.
- `scripts/check_b217.sh:221` — row V (`eq .State "pending"`) is satisfied by the state **badge** at `cluster.html:116`; the Approve button's own condition (`cluster.html:147`) can be deleted and V stays green — match the condition inside the block holding the `/admin/cluster/node/approve` form.
- `scripts/check_b218.sh:56` — row C only greps the literal `TestParseRolesCSV` in a test file, so an emptied test body passes; `check_b224.sh:114`, `check_b225.sh:127/132`, `check_b225_1.sh:128`, `check_b225_2.sh:116`, `check_b226.sh:200`, `check_b219.sh:106`, `check_b220.sh:141`, `check_b216.sh:182`, `check_b223.sh:83` are the same class (name/count presence instead of execution).
- `scripts/check_b212.sh:128` and `:100`, `scripts/check_b219.sh:47`, `scripts/check_b214.sh:91`, `scripts/check_b211.sh:144`, `scripts/check_b223.sh:84` — satisfied by the symbol's **definition**, a doc comment, any `go func()`, a bare `DO $$`, or an unrelated idiom respectively — scope each grep to the call site it names.
- `scripts/check_b211.sh:128` — `awk … | grep -q "state ="` under `set -uo pipefail`: if `grep -q` exits at the first match, awk dies with SIGPIPE (141), `pipefail` makes the pipeline non-zero and the **else** branch reports `PASS` on the very "state = present" regression the row exists for — capture the block first, then match.
- `scripts/check_b203.sh:222` (same shape at `check_b204.sh:175`, `check_b205.sh:220`, `check_b206.sh:220`, `check_b207.sh:237`, `check_b208.sh:200`) — `check "go build/vet/tests skipped (no go in PATH)" ok` — the only executable half of each script is reported as a PASS when Go is missing — emit `SKIP`.
- `scripts/check_b203.sh:215` — the `go test -run '…|TestDBSwap|…'` filter has an alternative that matches **no** test in `./internal/db/` or `./internal/watchdog/` (the real names are `TestNewDBSwap_DefaultsApplied` etc.) — that alternative and the watchdog swap contract it was meant to select are dead — use the real names.
- `scripts/check_b203.sh:75,:110` and `scripts/check_b206.sh:105` — `\*sql\.DB`, `sql\.ErrConnDone` and `Current() \*sql.DB` are satisfied by doc comments (`swapdb.go:69`, `:141`, `internal/db/dbsource.go:22`) — awk the struct/interface body.
- `scripts/check_b208.sh:90-94` — `grep -A3 'type Service struct' internal/feature/admin/service.go | grep -qE 'DB[[:space:]]+\*sql\.DB'` — the struct starts at `service.go:93` and the `DB` field is `:103`, so the window (93-96) can never contain it: "Service.DB is DBSource (not *sql.DB)" is a permanent PASS — assert the field positively inside the struct body.
- `scripts/check_b208.sh:143,:148` — the two `-A50`/`-A100` windows are anchored at the map literal (`ha.go:194`), not at the queries, and both `FROM` statements (`:204`, `:230`) sit inside **both** windows — the `cluster_audit` assertion adds 100 lines of slack — awk each query.
- `scripts/check_b210.sh:96-99,125-128,155-158,171-174` — the four "main.go passes ResettableDB to `<svc>`" pairs are `<ctor> && file_grep "DB:\s+d,"`; the second grep is unscoped, so **any** `DB: d,` in `main.go` (e.g. `metrics.DBPoolSource{DB: d, …}` at `:1218`) satisfies all four — copy `check_b208.sh:106`'s scoped form.
- `scripts/check_b210.sh:89-93` — the auth "0 remaining `s.DB.method` sites" scan covers only `internal/feature/auth/service.go`, and `auth_remaining=${auth_remaining:-0}` turns a grep failure into PASS — scan the package and fail on a non-numeric result.
- `scripts/check_b202_5.sh:100-107` — "SSHDumpTransport has 5 fields" greps `^\s+Field\s+(string|int)` across the **whole file**, not the struct — scope with awk. `:130-137` — the five `SKYGATE_DBMIGRATE_SSH_*` names are substring greps satisfied by a comment — require the `os.Getenv` reads.
- `scripts/check_b205.sh:103,:111` — the `-A30`/`-A50` windows from `runClusterInvite`/`runClusterJoin` (`cmd/skygate/cluster.go:152,:254`) reach only the comments at `:173` and `:295-296`; the real `cluster.IssueInvite` (`:189`) and `http.Post("…/api/cluster/join")` (`:316`) are **outside** the windows — awk the function bodies.
- `scripts/check_b206.sh:172-183` — four `grep -A200 'func .s \*Sampler. collect'` windows: the anchor regex also matches `collectSQLite` (`internal/feature/healthz/db_health.go:672`), so the effective window runs from `:542` to EOF and any token can be satisfied from another function — awk the `collect` body.
- `scripts/check_b206.sh:167` — the `-A10` window from `tick()` (`db_health.go:429`) reaches `s.src.Current()` exactly at `:439` (zero margin) — one comment inside `tick()` makes it a false FAIL.
- `scripts/check_b207_fix.sh:84` and `:91` — `sudo -u postgres` is satisfied by the usage comment (`clear_test_dsn.sh:30`) and "as the final step" is only a presence test (the comment at `b208_verify.sh:14` would pass) — grep the `PSQL=` assignment and assert the last non-comment line.
- `scripts/check_b209.sh:116-119` and `:88-95` — "e2e exits 0 on pass + 1 on fail" is two presence greps (`exit 0`/`exit 1` exist at `b209_e2e.sh:257,:260`) and "has all 8 phases in order" only greps the `[Phase N]` echo labels — an inverted guard or a gutted phase body passes; and the e2e itself is never executed by the gate — run it behind a `psql`-available gate.
- `scripts/check_b237_7.sh:46-50`, `scripts/check_b237_2.sh:89-93`, `scripts/check_b236.sh:57,:63,:66`, `scripts/check_b237.sh:111`, `scripts/check_b237_22.sh:207-208`, `scripts/check_b237_10.sh:176-180` — contracts satisfied by a **comment** (`reconciler.go:295`, `derp.go:599`, `tailscale.go:195/779/817/1753/1761`, `derp_apply_headscale_b237.go:210/223`, `cdn.go:196/248`), by the guard's own definition (`tailscale.go:1870`), or by the regressed form of the call itself (`GenerateManualSteps.*current.*target`) — anchor on the executable expression.
- `scripts/check_b237_21.sh:135-143` — the failure branch needs `! grep -qE 'maskSecret\(creds\.Password\)'`, but C.1 (`:102-106`) *requires* that string in the same file — the condition is always false and the `bad` is unreachable — drop the maskSecret clause or scope the grep to the set-verb body.
- `scripts/check_b237_19.sh:98-100` — computes `n_user_errors`/`n_forbidden` and only `echo "INFO …"`s them — add `[ "$n_user_errors" = 0 ] || bad …`.
- `scripts/check_b237_16.sh:127-132` — gates on `grep -q 'http\.StatusForbidden'` and then prints the count `n` in the `ok` message; `n` is never compared — compare it.
- `scripts/check_b237_22.sh:195-199` — `grep -qE 'cdn_group_id|cdn_group_marker' internal/db/migrations/*.sql` — that path has **no files** (0 `*.sql` under `internal/db`), so grep errors silently into the `else` and D.3 always PASSes — use `internal/db/migrations_*.go`.
- `scripts/check_b237_22.sh:229-233` — `if go test … 2>&1 | grep -qE 'FAIL|--- FAIL'; then bad; else ok` under `set -uo pipefail` (`:54`) — when tests really fail, `grep -q` exits at the first match, `go test` dies with SIGPIPE (141), `pipefail` makes the pipeline 141 and the `if` takes the **`ok`** branch: a genuine FAIL is reported as PASS — capture first, then match.
- `scripts/check_b237_22.sh:298-317` — `go build ./... 2>&1 | grep -q .` (and the `go vet`/`staticcheck` twins) — success is inferred from a non-zero pipeline, which is also what SIGPIPE or a missing tool produces, so a real build failure can print `ok "I.1 go build ./... is clean"` — use `if ! go build ./... >/dev/null 2>&1; then bad`.
- `scripts/check_b237_20.sh:54,69,82,83,97` and `scripts/check_b237_16.sh:103,112` — `staticcheck ./... 2>/dev/null | grep -cE '…' || true` compared with `= "0"` — a staticcheck that dies prints nothing, so "0 findings" is indistinguishable from "staticcheck did not run" — check the exit status before trusting the count.
- `scripts/check_b237_20.sh:194-201` — `grep -q '^ok'` over three packages with **no rc check** — one green package masks a FAIL in the others — use `check_b237_16.sh:182`'s `rc -eq 0 && grep -q '^ok'` form.
- `scripts/check_b235.sh:210-216` and `scripts/check_b237.sh:275-281` — success = `grep -q '^ok'` over `go test -run TestTemplateArgsMatchCatalog ./internal/handlers/...` — a renamed/deleted test prints `ok … [no tests to run]` and exits 0, so the contract PASSes with **zero tests run** — pin the test name or assert a non-zero test count.
- `scripts/check_b237_17.sh:190-194` and `scripts/check_b237_18.sh:255-259` — `grep -B1 -A2 'TD-9'/'TD-10' docs/ROADMAP.md | grep -qE 'DONE\b'` — the ±window merges the adjacent row (`docs/ROADMAP.md:225` TD-9 `**DONE**` satisfies the TD-10 row at `:226`), and `TD-9` is unanchored — anchor `^\| TD-N \|`.
- `scripts/check_b237_18.sh:86-90` and `:218-222` — the same inverted "no DELETE/TRUNCATE in `reconcile.go`" grep twice; both PASS when the file is missing (only `:60-64` checks existence) — add `test -f` and delete the duplicate.
- `scripts/check_b232.sh:88` — the regex `'PGMigrations\[last\].Version.*<.*68|want >= 68'` matches the **`t.Errorf` message** at `internal/db/migration_b213_test.go:63`, not the guard at `:62` — a weakened guard still PASSes — `grep -qE 'if last\.Version < 68'`.
- `scripts/check_b232.sh:105` — banks `TestMigrateV068PG_PreFlightRefusesOnDuplicates`, a `t.Skip` stub (`migrations_v0_68_b232_test.go:81-83`) — require a non-skipped test.
- `scripts/check_b233.sh:107` — "the mutation test pins the audit" — `TestShapeDriftAudit_CatchesSyntheticOffender` (`migrations_audit_b233_test.go:435-476`) inlines a copy of the loop and sets `found` from the fixture alone (`:465`), so it is green even if the real audit is neutered — make the test call the audit function.
- `scripts/check_b227.sh:189` — the rate-limit-gate contract requires only `shouldAlert` within ±2 lines of `lastAlert[key] = now` (`internal/nodeownership/auto_alert.go:361`); the metric `Inc()` at `:315-317` (the thing it claims to keep outside the gate) is 44 lines away — assert the ordering explicitly.
- `scripts/check_b244.sh:99-108,111,119,129,137,148` — absence pins with `2>/dev/null || true` — deleting/renaming `PROJECT.md`, `README.md`, `docs/ru/README.md` or `AGENTS.md` makes every one PASS vacuously; `:156`/`:170` never assert `internal/i18n/catalog_bot.go` exists — add a `test -f` per pinned file.
- `scripts/check_b249.sh:87-92` — three contracts labelled "(RU)" only grep `catalog_update.go` for the key — a key present only in the EN map satisfies them — check the RU map explicitly.
- `scripts/check_b250.sh:91-99` — section C passes in **both** arms (`echo PASS` + `PASS=$((PASS+1))`); `bad()` is never called, so the "dark cards without the light-text fix" contract is unenforced — call `bad` for the non-empty list or delete the section.
- `scripts/check_b250.sh:58` — B1 (`grep -A1 'v0_17_0_note_title'`) starts at `acls.html:192`, so it never sees the card's own `color:` at `:191` — the "the dark card declares light text" contract is satisfied by the `<h3>`/`<p>` even if the card loses its colour — assert the card line.
- `scripts/check_b252.sh:122-123` — the "(RU + EN)" i18n rows are occurrence counts in one file with dots used as regex wildcards — both hits may be in the RU map — count per language block.
- `scripts/check_b252_derp_cert_sync.sh:85-95` — the column block is an awk range with a `\t`-based terminator truncated to 4000 chars: on one awk the range stops at the PG table (`migrations_v0_71_derp_cert_sync.go:93`) and the **SQLite chain** (`:126-145`) is never covered; on another it spills into it — either way a SQLite-only column drop/rename passes A — anchor the slice per chain.
- `scripts/check_b253_telegram_async.sh:39-44` — only asserts that `refreshProbeAsync` and `go s.refreshProbeAsync(` appear in `telegram.go` — re-adding a synchronous `cachedTelegramProbe` on the render path keeps every contract green while the page blocks again (the exact B253 regression) — assert the spawn precedes any probe in the render body. `:76,:79` pin gofmt's alignment literally — adding a longer field makes the check permanently red.
- `scripts/check_b258_1_auth_key_missing.sh:140` — the loop's second alternative matches as an unanchored substring for **every** probe, so `tests_present` can be 5 with four cases absent — anchor each probe to its own test name. `:154-162`/`:165-172` — the `go vet`/`go test` `else` branch prints `ok`, so a missing toolchain is reported as two PASSes.
- `scripts/check_b256_other_hs_user_ids_int_literal.sh:161-181` — contract K prints a heredoc and then `ok "K: live-state note printed"` — nothing is executed, so it PASSes on Windows and on a broken deployment — turn it into a real psql/journal probe. `:148-149` — J prints `ok` when `go` is missing.
- `scripts/check_b260_derp_status_collection.sh:277` — `ok "L: live-state note printed (run manually after deploy)"` — an unconditional `ok` after a `cat <<NOTE` block — replace with a WebSocket-upgrade probe (assert `101`) or drop it. `:245-249` — deployment gate K prints `ok` when the compose file is absent and when `CI` is set.
- `scripts/check_b260_derp_status_collection.sh:152-157` and `scripts/check_b260_2_derper_in_docker.sh:163-168` — 10/20-line `-A` windows around 11-line functions (`resolveDERPHostname`, `resolveDERPPort`) — one added comment flips correct code to FAIL, while a differently-spaced env-first rewrite passes — awk the function body.
- `scripts/check_b_duplicate_users.sh:73-90` — when `python3` is absent the result file is empty, `FIRST_LINE` is empty and the script falls through to `FAIL A: headscale has duplicate user names` + `exit 1` — a fabricated regression — guard with `command -v python3` (SKIP).
- `scripts/check_b_node_owner_map_orphans.sh:71-79` — psql stderr is swallowed (`:71`), `ORPHAN_COUNT=$(… | grep -c '.' || true)` (`:73`) is `0` for empty input, and `:75-78` then prints `PASS A: 0 orphan rows … (all  rows point at live headscale users)` with `TOTAL` empty — a wrong password, missing DB or unreachable PG reports a clean map — capture the psql rc and FAIL with "query error" (exit 2).
- `scripts/check_b_reconcile_audit_writes.sh:121-143` — PASS C, the **only** direct detector of the B243 `SQLSTATE 42601` bug, is nested inside the PASS-A branch, so it is unreachable whenever no new audit row appears; and a healthy tailnet writes no row at all (`internal/headscale/reconcile.go:232-235` writes the summary only when the duplicate set changed), so a correct system can FAIL at `:150` with the false "exact B243 bug pattern" text — run the `:135` grep unconditionally after the health wait.
- `scripts/check_b_reconcile_audit_writes.sh:135-137` — `docker logs … | grep -c "SQLSTATE 42601" || true` compared with `= "0"` — an empty/failed log source reads as "0 errors → B243 SQL fix verified" — check the `docker logs` rc. `:63-70` — `BACKEND` is computed and never read, so the `:69` FAIL is unreachable and the script always talks to hardcoded PG.
- `scripts/check_b261_native_self_update.sh:597,:637` — calls `skip "…"` but only `ok`/`fail`/`hdr` are defined (`:59-61`) — under `set -euo pipefail` (`:55`) a missing dependency yields `command not found` → **exit 127**, a hard FAIL where AGENTS rule 1 requires SKIP — define `skip()`. `:104` — a renamed `renderRequest` makes the `sed` range empty and the inverted contract PASS vacuously. `:213-218,:260-267` — `ok "G: skipped sh -n …"` / `ok "H: skipped (no go …)"` report dependency-unavailable paths as PASS.

Unregistered but in-range (they can never run at all):

- `scripts/check_b200.sh`, `check_b201.sh`, `check_b202.sh`, `check_b238.sh` — exist on disk, referenced by no gate (verified against `verify_pre_deploy.sh` and `verify_post_deploy.sh`) — the B200–B202 DB-migration/HA framework and the B238 portal_users surface have no gate coverage — register them or delete them.
- `scripts/check_b255_telegram_async.sh:81-87` — the "page no longer blocks" extractor clamps bodies at 80 lines while `loadTelegramUIState` is 114 lines (`internal/feature/admin/telegram.go:300-414`) — a re-introduced synchronous probe past line 80 is invisible. `:199-201` genuinely runs `go test` (the run's filter matches `telegram_b255_test.go`), so the suspicion of a fake `ok` there is **not** confirmed.
- `scripts/check_b257_adopt_devices.sh`, `check_b259_tailscale_toggle.sh`, `check_b258_tailscale_disabled.sh`, `check_b260_2_derper_in_docker.sh` — no gate registration; between them they hold the only contracts for the adopt route/handler (`main.go:2014`, `adopt_devices.go`), the UI Tailscale toggle, the legacy `SKYGATE_TS_AUTHKEY_PATH` fallback and the whole derper-in-docker packaging. `check_b257_adopt_devices.sh:130-149`, `check_b259_tailscale_toggle.sh:145-158` and `check_b258_tailscale_disabled.sh:120-133` put their entire executable section in the `else` of `[ -z "$GO" ]` and print a message instead of `SKIP` (exit 0).
- `scripts/check_b258_tailscale_disabled.sh:86` — asserts `or (not .State.Available) .State.AuthKeyDisabled`, but the only template occurrence (`tailscale.html:363`) is `… .State.AuthKeyDisabled .State.AuthKeyMissing}}}` — the contract can **never pass**, so registering this script as-is would paint the gate red — fix the pattern first.
- `scripts/check_b259_tailscale_toggle.sh:92-94` — the "disable handler persists `/dev/null`" contract greps the same literal as `:79` (`db.SetGlobalSetting(s.dbc(), tailscaleAuthKeyPathDBKey, newPath)`), which is the enable-path line — it never mentions `/dev/null` and cannot distinguish disable from enable.
- `scripts/check_b260_2_derper_in_docker.sh:216-217` — `A || B` where `B` is `\$\{DERP_VERIFY_CLIENTS_URL:-.*\}` — the `.*` subsumes `:-false`, so an empty default passes the "non-empty default" contract.

## Runtime invariants with no behavioural contract

- **A tag written to `node_owner_map` actually reaches the headscale node.** Within B200–B262 no contract compares `node_owner_map.tag` with the tags on the live node: `check_b_node_owner_map_orphans.sh:64` *selects* `tag` but filters only on `headscale_user_id`, `check_b227.sh:152` pins the `alertSink.ReportFailure(...)` call site as text, and `check_b251.sh` never exits non-zero. A user sees per-device exit rules silently stop matching (the B272 incident: the DB says `tag:dev-x`, `headscale nodes list` shows no tags). Proposed: live contract — for every `node_owner_map` row, `headscale nodes list -o json` must show that tag on that node id; FAIL naming the node/hostname/tag.
- **The ACL/tagOwners policy skygate generates is the one headscale is running.** `check_b251.sh:139-170` only proves `hujson.Standardize`/`unquotePolicyIfStringified` are *mentioned* in `internal/headscale/tags.go` (their definitions count), and the unit tests reached through B1 use an httptest handler that returns 200 unconditionally (`internal/headscale/tags_b251_test.go:93-94`), so a real `400 invalid or not permitted` or a `policy.mode: file` `500` is covered by nobody. A user sees devices lose their rules while the panel shows the policy applied. Proposed: read the live policy (`GET /api/v1/policy`) and compare it to the generated one with `headscale.PolicyEquivalent`, plus an `EnsureTagOwner` round-trip asserting the tag appears in the returned policy.
- **`/admin/tailscale` "set advertise routes" actually changes the container's tailscale prefs.** `check_b236.sh:57/63/66` are satisfied by comments and by a definition; nothing runs `tailscale set --advertise-routes=` and reads back `tailscale debug prefs`. A user sees the saved route list and a relay that advertises nothing (`--accept-routes` never applied → the "Telegram unreachable" class). Proposed: execute `tailscale set …` against a fake prefs file (or the real container) and assert the resulting pref JSON.
- **The HA elector actually detects a dead node and writes `failover_recommend`.** `check_b204.sh` tests the pure `nextState`; `check_b209.sh:72-126` only greps `scripts/b209_e2e.sh`, which needs psql and has no SKIP path (`b209_e2e.sh:110-116` exits 1 when PG is unavailable) — so the only end-to-end HA contract never runs. A user sees a dead primary that nobody fails over. Proposed: register a live-gated run of `b209_e2e.sh` (SKIP when psql is unavailable) asserting the `failover_recommend` audit row after backdating `last_seen_at`.
- **The reconcile cron writes its `audit_log` row on PostgreSQL.** The only contract (`check_b243-reconcile-audit`) evaluates its `SQLSTATE 42601` detector only if a row appeared, and a healthy tailnet produces no row. A user sees "the cycle ran" in the logs with no audit trail — the silent-failure class B243 was fixed for. Proposed: assert the absence of `SQLSTATE 42601` after a restart **unconditionally**, and force a row with a synthetic duplicate fixture.
- **A failover / backup / DB-health alert is actually emitted.** `check_b225.sh`, `check_b225_1.sh`, `check_b225_2.sh` never run a test (and the latter two never even compile), and `check_b226.sh` never runs the metrics tests: the alert verbs (`DEGRADED`, `recovered`), the detector selection in `tick()`, the metric label sets and the Telegram sends are asserted as literals. A user gets no notification for a failed failover, a stalled backup or a degraded DB because the alert path regressed silently. Proposed: run `internal/db` + `internal/watchdog` + `internal/metrics` tests from these scripts, and assert the sink receives one alert per transition with a fake notifier.
- **The DERP certificate is renewed before it expires.** `check_b252_derp_cert_sync.sh:130-134` greps `expiryFromCert`, `SyncOne`, `npmLogin`, `npmDownloadCert`, `reloadDerper` as names; no test or probe calls them (the only nearby coverage is `certificates_test.go:283`'s path test). A user sees the relay fail TLS with an expired certificate while `/admin/derp` reports the sync as configured. Proposed: a unit contract over `expiryFromCert` with a fixed `NotAfter` (30-day boundary) plus a live probe asserting the served certificate's `NotAfter` is in the future.
- **`/admin/derp` reports derper's real state.** `check_b260_derp_status_collection.sh` is greps plus an unconditional `ok`, and `check_b260_2_derper_in_docker.sh` is unregistered. A user sees "DERPER-SERVICE: stopped" on a running relay. Proposed: a gated WebSocket-upgrade probe asserting `101 Switching Protocols` and a STUN binding round trip decoding XOR-MAPPED-ADDRESS.
- **The preferred-exit reconciler's DB write path works.** `check_b229.sh` executes only the pure planner + rate limiter; `applyReconcilerChange` (`internal/feature/exit_rules/reconciler.go:593`) is never run. A user's preferred-relay change is computed and never persisted, so per-CIDR `via` pins stay stale. Proposed: a sqlite-backed test asserting the `device_exit_node_prefs`/`device_rules` rows after `applyReconcilerChange`.
- **`skygate init`/`join`/`migrate` really bootstrap a node.** B211–B213 assert source text, run unit tests and print `--help`; the dispatchers are never executed. A user follows the printed NEXT STEPS and the standby never joins. Proposed: run the built binary with `init status` against a temp SQLite DB and assert the exit code and printed state.
- **The `/admin/cluster` action buttons map to the right handler for the right state.** B216–B223 assert template strings; `check_b217.sh:221` is satisfied by the state badge rather than the button guard. A user sees Approve on a non-pending node or a Drain button that 404s. Proposed: render `cluster.html` with a fixture `clusterNode` per state and assert which forms appear.

## Notes

- **The single worst defect in the slice is `scripts/check_b251.sh`**: it cannot fail. `bad()` (`:57`) has no counter and the file has no `exit` at all (last line `:173`), so the gate prints `PASS B251` and **discards the entire output** (`verify_pre_deploy.sh:190-192`). The B251/B245 surface (reserved `skygate-host` name, strict `isInfraNode` equality, hujson/stringified-policy parsing) is unguarded in production, and the `run_check` description quotes a "Live state:" narrative that no contract implements.
- **Nine in-range check scripts are registered in no gate**: `check_b200/201/202/238.sh`, `check_b255_telegram_async.sh`, `check_b257_adopt_devices.sh`, `check_b258_tailscale_disabled.sh`, `check_b259_tailscale_toggle.sh`, `check_b260_2_derper_in_docker.sh` (verified against both `verify_pre_deploy.sh` and `verify_post_deploy.sh`; the only other references are two filename strings in `check_b303_ownerless_device.sh:198,203` and a doc example in `scripts/README.md:62`). Five of them cover whole features (async Telegram page, device adoption, Tailscale disable/toggle UI, derper-in-docker packaging) that no other script touches, and one (`check_b258_tailscale_disabled.sh:86`) is currently a permanent false FAIL, so it cannot simply be registered as-is.
- **Style distribution.** Of the 68 entries: 44 `mixed`, 20 `structural`, 3 `live-only`, 1 no-op (B234). Every `live-only` entry belongs to the B243 family and lives behind `scripts/lib/skip_if_no_docker.sh:34-36`, which exits 0 when the docker daemon is unreachable — on the operator's Windows checkout **and in CI** (`.github/workflows/ci.yml:123-132`) those three contracts, the only ones that ever inspect live state, are counted as PASS. Of the 44 `mixed` entries, 5 (`check_b224`, `b225`, `b225_1`, `b225_2`, `b226`) have no `go test` at all in their executed half; `b225_1`/`b225_2` do not even run `go build` and have no `go` SKIP line.
- **The `[skip]` rows that cannot fail** are a systematic pattern: `check_b218.sh:168` (row N), `check_b220.sh:149`, `check_b221.sh:205`, `check_b222.sh:242`, `check_b223.sh:191,209`, `check_b224.sh:137,144`, `check_b225.sh:139,146`, `check_b225_1.sh:140`, `check_b226.sh:212` — each prints `[skip]` on the non-matching branch without touching the FAIL counter, so the gate records PASS. The same shape appears in `check_b200`-family scripts outside this slice.
- **The `go`-missing SKIP lines hide the whole executable half**: `check_b211.sh:167,178,206`; `b212:148,160,184`; `b213:143,154,178`; `b214:138,149,168`; `b215:145,156`; `b216:210,221`; `b217:250,261`; `b218:182,193`; `b219:146,157`; `b220:163,174`; `b221:219,230`; `b222:238,256`; `b223:187,205`; `b224:158`; `b225:160`; `b226:226`. On a host without `go` the scripts still exit 0, so the gate reports PASS from greps alone.
- No slice script has a SIGPIPE `pipefail` false-FAIL of the AGENTS trap-9 shape except the `check_b237_*` family (`check_b237_7.sh:129,142`; `check_b237_10.sh:270,283,296`; `check_b237_18.sh:227`; `check_b237_19.sh:185`; `check_b237_21.sh:171,208`), where `check_b237_22.sh:229` turns it into the opposite hazard: a real test failure reported as PASS. In the HA/cluster family the trap appears as a false-PASS (`check_b211.sh:128`) and as spurious FAILs (`check_b221.sh:112,133`); `check_b237_2.sh:179,191`, `check_b237_16.sh:182` and `check_b237_20.sh:194` are the fixed capture-then-match references — and `check_b237_20.sh:194-201` still lacks the rc check.
- The `n=$(grep -c '^func Test' … || echo 0)` idiom (`check_b216.sh:156,179`, `check_b217.sh:199,235`, `check_b219.sh:97,106`, `check_b220.sh:125,141`, `check_b221.sh:69,182`, `check_b222.sh:202`, `check_b223.sh:171`, `check_b225_1.sh:129`, `check_b225_2.sh:117`) emits `0\n0` because `grep -c` prints `0` **and** exits 1, so `[ "$n" -ge N ]` raises `integer expression expected`. The outcome is still a FAIL, but the gate shows a bash error instead of the contract text — use `|| true` plus `${n:-0}` (`check_b224.sh:94-96` has the `head -1` fix).
- Several contracts sit at exactly today's threshold (14, 8, 7, 6, 3 tests / 9 actions), so the first legitimate addition flips them red for a non-regression: `check_b237_20.sh:116-121` (`>= 14` while the message says 17), `check_b220.sh:141`, `check_b225_1.sh:128`, `check_b225_2.sh:117`, `check_b226.sh:200`, `check_b219.sh:106`, `check_b216.sh:182`, `check_b217.sh:105`. Windows with zero or near-zero margin do the same in the other direction (`check_b206.sh:167`, `check_b224.sh:107,121`, `check_b225.sh:102/113/120`, `check_b252.sh:51-53`, `check_b237_24.sh:67-74`).
- Two contracts are stale in ways that mislead the operator: `check_b229.sh:20-22` (and the gate description at `verify_pre_deploy.sh:3665`) say preferred-exit reconciliation is "default off / DRY-RUN", while the code is default-**ON** opt-out (`reconciler.go:290-292`) — the operator believes writes are disabled when they are live; and `check_b227.sh:26` cites `nodeownership.go:633` for the `ReportFailure` call site, which is actually `:684`.
- `scripts/check_b261_native_self_update.sh` is the only entry in the slice that really exercises an artifact end to end (installer checksum rehearsal with a stubbed `curl`, applier validation runs), and it still cannot cover the privileged swap: its own header concedes the root-only path is only "covered by the live canary recorded in docs/ROADMAP.md §12.16". Its undefined `skip()` means that on a host without `tar`/`sha256sum`/`install` those rehearsal sections report exit 127 instead of SKIP.
- `check_b203.sh` is registered twice (B203 at `:3519-3520`, B203.1 at `:4272-4273`) and `check_b202_5.sh` twice (`:3522`, `:4251`), so the gate links and tests the same packages twice per run; the B203.1-specific contracts cannot be told apart from B203's in the output.
