// Code generated by split_i18n.py — refactor-v0.30 Phase C.
// DO NOT EDIT THIS FILE BY HAND — run scripts/split_i18n.py to
// regenerate from the source-of-truth (catalog_common.go etc.)
//
// Per-feature i18n keys for the user_subnet feature. The keys are
// declared as package-level maps; the glue file (i18n.go)
// merges them in New().

package i18n

// user_subnet feature — 73 ru keys, 73 en keys.
// Top-level prefixes: user_subnet

var ruUserSubnet = map[string]string{
	"user_subnet.title"                           : "Personal subnet",
	"user_subnet.subtitle"                        : "выделение personal CIDR + sidecar provisioning",
	"user_subnet.no_subnet_title"                 : "Subnet не выделена",
	"user_subnet.no_subnet_body"                  : "У этого пользователя пока нет personal subnet. Нажмите кнопку ниже, чтобы выделить — будет назначен детерминированный CIDR <code>10.0.&lt;uid&gt;.0/24</code>. Реальный sidecar provisioning — в v0.16.1.",
	"user_subnet.allocate_button"                 : "Выделить subnet",
	"user_subnet.status_title"                    : "Текущее состояние",
	"user_subnet.cidr"                            : "CIDR",
	"user_subnet.status"                          : "Статус",
	"user_subnet.bits"                            : "Размер",
	"user_subnet.plane"                           : "Control plane",
	"user_subnet.router_hostname"                 : "Router hostname",
	"user_subnet.router_node_id"                  : "Headscale node_id",
	"user_subnet.created_at"                      : "Создано (unix)",
	"user_subnet.updated_at"                      : "Обновлено (unix)",
	"user_subnet.test_button"                     : "Sanity check",
	"user_subnet.disable_button"                  : "Disable",
	"user_subnet.test_results"                    : "Результат sanity check",
	"user_subnet.provision_button"                : "Выдать preauth key",
	"user_subnet.download_button"                 : "Скачать bundle (.tar.gz)",
	"user_subnet.download_button_help"            : "Скачивает setup.sh + README + commands.txt с уже вставленным preauth. Юзер scp'ит на свой роутер, untar, запускает.",
	"user_subnet.preauth_title"                   : "Preauth key для sidecar",
	"user_subnet.preauth_help"                    : "Передайте этот ключ пользователю (или вставьте в его sidecar). На хосте должна быть запущена <code>tailscale</code> ≥ 1.34. После <code>tailscale up</code> skygate автоматически (<30с) approve-ит маршрут и переведёт статус в <code>active</code>.",
	"user_subnet.preauth_key"                     : "Key",
	"user_subnet.preauth_hostname"                : "Hostname",
	"user_subnet.preauth_routes"                  : "Routes",
	"user_subnet.preauth_expires"                 : "Expires",
	"user_subnet.preauth_note"                    : "Ключ одноразовый, 1 час TTL. После <code>tailscale up</code> используется и больше не работает. Для повторной выдачи нажмите <i>Выдать preauth key</i> ещё раз.",
	"user_subnet.sharing_title"                   : "Cross-user sharing",
	"user_subnet.sharing_help"                    : "Расшарьте свой personal subnet для другого юзера. Доступ однонаправленный: вы → он. В ACL добавятся <code>10.0.&lt;ваш&gt;.0/24</code> в <code>dst</code> его per-user правила. ACL пере-применяется автоматически.",
	"user_subnet.sharing_i_shared"                : "Я расшарил",
	"user_subnet.sharing_shared_with"             : "Мне расшарили",
	"user_subnet.sharing_none_i"                  : "никому",
	"user_subnet.sharing_none_with"               : "никто",
	"user_subnet.sharing_grantee_label"           : "Имя пользователя",
	"user_subnet.sharing_grantee_placeholder"     : "username (lowercase)",
	"user_subnet.share_button"                    : "Расшарить",
	"user_subnet.revoke_button"                   : "Отозвать",
	"user_subnet.magicdns_button"                 : "DNS имена",
	"user_subnet.magicdns_sidecar_label"          : "Sidecar FQDN",
	"user_subnet.magicdns_short_label"            : "Короткое имя",
	"user_subnet.magicdns_wildcard_label"         : "Per-device паттерн",
	"user_subnet.magicdns_note"                   : "Работает автоматически когда в headscale включён <code>dns.magic_dns: true</code> и <code>dns.base_domain: tsnet.skynas.ru</code>. Sidecar регистрируется с <code>--hostname=skygate-subnet-&lt;username&gt;</code>, что автоматически даёт FQDN выше. Per-device записи работают если в headscale настроен <code>dns.search_domains</code>.",
	"user_subnet.preferred_exit_title"            : "Preferred exit-node (v0.28.1)",
	"user_subnet.preferred_exit_help"             : "<b>Preferred exit-node</b> ограничивает через какую exit-ноду ходит ваш трафик. headscale enforces это через <code>via</code> в grants — выбрав <code>emilia</code> в GUI, ваш трафик физически пойдёт через <code>emilia</code>, даже если Tailscale-клиент показывает другие варианты.",
	"user_subnet.preferred_exit_label"            : "Предпочитаемая exit-нода",
	"user_subnet.preferred_exit_none"             : "не выбрана (любая доступная)",
	"user_subnet.preferred_exit_button"           : "Сохранить",
	"user_subnet.preferred_exit_currently"        : "Сейчас активно",
	"user_subnet.preferred_exit_default_help"     : "Не выбрано — вы можете ходить через любую доступную exit-ноду (emilia, sharlotta, karolina). Чтобы закрепить трафик за конкретной нодой, выберите её в выпадающем списке и нажмите Сохранить.",
	"user_subnet.preferred_exit_via_label"        : "Strict (via)",
	"user_subnet.preferred_exit_via_help"         : "когда ВКЛ, headscale ACL пинит трафик пользователя к этой exit-ноде. Старые Tailscale клиенты (Android) могут отвергнуть такую policy.",
	"user_subnet.no_exit_nodes"                   : "Нет ни одной exit-ноды в headscale. Сначала добавьте ноду с <code>tag:exit-node</code> через /admin/exit-nodes.",
	"user_subnet.v0_16_0_note_title"              : "Что в v0.16.0 vs v0.16.1",
	"user_subnet.v0_16_0_note_body"               : "<b>v0.16.0 (этот релиз):</b> schema + CIDR allocator + admin UI + бот <code>/mysubnet</code>. Кнопка <i>«Выделить subnet»</i> создаёт запись в <code>user_subnets</code> со статусом <code>pending</code>.<br><br><b>v0.16.1:</b> реальный sidecar контейнер + headscale preauth + node tagging + route approval. После deploy v0.16.1 кнопка <i>«Sanity check»</i> покажет router node_id, а бот <code>/mysubnet</code> покажет статус <code>active</code> вместо <code>pending</code>.",
	"user_subnet.use_cases_title"                 : "Зачем это нужно пользователю",
	"user_subnet.use_cases_body"                  : "<b>Sub-router</b> — это Tailscale-узел на стороне пользователя, который анонсирует <code>10.0.&lt;uid&gt;.0/24</code> в tailnet. После approve-а маршрута любой tailnet-клиент с <code>--accept-routes</code> видит его домашнюю сеть как часть tailnet. Типичные сценарии:<ul style=\"margin:4px 0 4px 18px;padding:0\"><li><b>Домашний NAS</b> (Plex, Synology, TrueNAS) — доступ с телефона в дороге.</li><li><b>Умный дом / IoT</b> (Home Assistant, MQTT, камеры) — остаётся в LAN, виден только через tailnet.</li><li><b>Серверная в офисе</b> (Proxmox, NAS, принтеры) — команда получает доступ без открытия портов.</li><li><b>Семейный доступ</b> — фото / видео на родительском NAS видны детям с планшетов без админки.</li><li><b>Лаб / dev</b> — VMs, тестовые БД на <code>10.0.&lt;uid&gt;.0/24</code>, доступные с ноутбука откуда угодно.</li><li><b>Кросс-сайт бэкап</b> — два физических сайта со своими LAN соединяются через tailnet для репликации.</li></ul>Если у пользователя всего 1-2 устройства — лучше поставить Tailscale на каждое напрямую, subnet-router нужен начиная с 5+ устройств или когда часть устройств не может запустить Tailscale (принтер, ESP32, старая NAS).",
	"user_subnet.onboard_title"                   : "Как подключить пользователя",
	"user_subnet.onboard_step1"                   : "<b>1. Выдели subnet</b> (если ещё не выделен). Кнопка <i>«Выделить subnet»</i> выше создаёт запись в <code>user_subnets</code> со статусом <code>pending</code> и CIDR <code>10.0.&lt;uid&gt;.0/24</code>.",
	"user_subnet.onboard_step2"                   : "<b>2. Дай пользователю preauth</b>. Два варианта: <ul style=\"margin:4px 0 4px 18px;padding:0\"><li>Кнопка <i>«Скачать bundle (.tar.gz)»</i> — выдаёт tar.gz с <code>setup.sh</code> + <code>README.md</code> + <code>commands.txt</code> + <code>CIDR.txt</code>. Юзер scp-ит на свой роутер, untar, запускает <code>sudo bash commands.txt</code>.</li><li>Кнопка <i>«Выдать preauth key»</i> — выдаёт только команду <code>tailscale up</code> (вставляется в чат).</li></ul>Оба варианта выдают preauth с TTL 1 час, single-use, тег <code>tag:subnet-router</code>.",
	"user_subnet.onboard_step3"                   : "<b>3. Дождись auto-approve</b>. Через ~30 сек sidecar увидит ноду в headscale, одобрит маршрут <code>10.0.&lt;uid&gt;.0/24</code>, обновит <code>user_subnets.router_node_id</code> и переведёт статус в <code>router_active</code>. Статус-пилл в админке обновится на следующей загрузке <code>/my/devices</code>.",
	"user_subnet.docs_link_text"                  : "Полная инструкция для пользователя (README, use-cases, troubleshooting)",
	"user_subnet.column_header"                   : "Subnet",
	"user_subnet.cell_none"                       : "—",
	"user_subnet.cell_pending"                    : "pending",
	"user_subnet.cell_active"                     : "active",
	"user_subnet.cell_disabled"                   : "disabled",
	"user_subnet.open_button"                     : "Subnet",
	"user_subnet.cell_router_active"              : "router active",
	"user_subnet.status_pill_active_title"        : "Subnet активна: у пользователя есть устройства в tailnet",
	"user_subnet.status_pill_pending_title"       : "Subnet не активна: пользователь ещё не добавил ни одного устройства",
	"user_subnet.status_pill_router_active_title" : "Subnet-router поднят: 10.0.<uid>.0/24 реально анонсируется в tailnet через tag:subnet-router",
	"user_subnet.status_pill_disabled_title"      : "Subnet отключена администратором",
	"user_subnet.status_explainer_v0_22_3"        : "<b>v0.22.3:</b> статус <code>active</code> теперь означает «у пользователя есть устройства в tailnet», а не «subnet-router поднят». <code>router active</code> — бонус-статус, появляется когда пользователь дополнительно поднял subnet-router для маршрутизации своей домашней сети. <code>pending</code> — только для новых пользователей, у которых ещё нет устройств.",
}

var enUserSubnet = map[string]string{
	"user_subnet.title"                           : "Personal subnet",
	"user_subnet.subtitle"                        : "personal CIDR allocation + sidecar provisioning",
	"user_subnet.no_subnet_title"                 : "No subnet allocated",
	"user_subnet.no_subnet_body"                  : "This user has no personal subnet yet. Click the button below to allocate one — the deterministic CIDR <code>10.0.&lt;uid&gt;.0/24</code> will be assigned. The actual sidecar provisioning ships in v0.16.1.",
	"user_subnet.allocate_button"                 : "Allocate subnet",
	"user_subnet.status_title"                    : "Current state",
	"user_subnet.cidr"                            : "CIDR",
	"user_subnet.status"                          : "Status",
	"user_subnet.bits"                            : "Bits",
	"user_subnet.plane"                           : "Control plane",
	"user_subnet.router_hostname"                 : "Router hostname",
	"user_subnet.router_node_id"                  : "Headscale node_id",
	"user_subnet.created_at"                      : "Created (unix)",
	"user_subnet.updated_at"                      : "Updated (unix)",
	"user_subnet.test_button"                     : "Sanity check",
	"user_subnet.disable_button"                  : "Disable",
	"user_subnet.test_results"                    : "Sanity check result",
	"user_subnet.provision_button"                : "Issue preauth key",
	"user_subnet.download_button"                 : "Download bundle (.tar.gz)",
	"user_subnet.download_button_help"            : "Downloads setup.sh + README + commands.txt with the preauth key already filled in. User scps to their router host, untars, and runs.",
	"user_subnet.preauth_title"                   : "Sidecar preauth key",
	"user_subnet.preauth_help"                    : "Hand this key to the user (or paste into their sidecar). The host needs <code>tailscale</code> ≥ 1.34. After <code>tailscale up</code>, skygate auto-approves the route within ~30s and flips status to <code>active</code>.",
	"user_subnet.preauth_key"                     : "Key",
	"user_subnet.preauth_hostname"                : "Hostname",
	"user_subnet.preauth_routes"                  : "Routes",
	"user_subnet.preauth_expires"                 : "Expires",
	"user_subnet.preauth_note"                    : "Single-use, 1h TTL. After <code>tailscale up</code> it's consumed. Click <i>Issue preauth key</i> again to issue a fresh one.",
	"user_subnet.sharing_title"                   : "Cross-user sharing",
	"user_subnet.sharing_help"                    : "Share your personal subnet with another user. One-directional: you → them. The ACL gets <code>10.0.&lt;your-uid&gt;.0/24</code> appended to their per-user dst list. ACL is re-applied automatically.",
	"user_subnet.sharing_i_shared"                : "I shared with",
	"user_subnet.sharing_shared_with"             : "Shared with me by",
	"user_subnet.sharing_none_i"                  : "nobody",
	"user_subnet.sharing_none_with"               : "nobody",
	"user_subnet.sharing_grantee_label"           : "Username",
	"user_subnet.sharing_grantee_placeholder"     : "username (lowercase)",
	"user_subnet.share_button"                    : "Share",
	"user_subnet.revoke_button"                   : "Revoke",
	"user_subnet.magicdns_button"                 : "DNS names",
	"user_subnet.magicdns_sidecar_label"          : "Sidecar FQDN",
	"user_subnet.magicdns_short_label"            : "Short name",
	"user_subnet.magicdns_wildcard_label"         : "Per-device pattern",
	"user_subnet.magicdns_note"                   : "Resolves automatically when headscale has <code>dns.magic_dns: true</code> and <code>dns.base_domain: tsnet.skynas.ru</code>. The sidecar registers with <code>--hostname=skygate-subnet-&lt;username&gt;</code> which auto-resolves to the FQDN above. Per-device records work if headscale has <code>dns.search_domains</code> configured.",
	"user_subnet.preferred_exit_title"            : "Preferred exit-node (v0.28.1)",
	"user_subnet.preferred_exit_help"             : "<b>Preferred exit-node</b> pins which exit-node your traffic uses. headscale enforces this via <code>via</code> in the grants — picking <code>emilia</code> in the GUI routes your traffic physically through <code>emilia</code>, even if the Tailscale client advertises other options.",
	"user_subnet.preferred_exit_label"            : "Preferred exit-node",
	"user_subnet.preferred_exit_none"             : "no preference (any available)",
	"user_subnet.preferred_exit_button"           : "Save",
	"user_subnet.preferred_exit_currently"        : "Currently active",
	"user_subnet.preferred_exit_default_help"     : "No preference set — your traffic may exit through any available exit-node (emilia, sharlotta, karolina). Pick one in the dropdown to pin your traffic.",
	"user_subnet.preferred_exit_via_label"        : "Strict (via)",
	"user_subnet.preferred_exit_via_help"         : "when ON, headscale ACL pins this user's traffic to the chosen exit-node. Older Tailscale clients (Android) may reject this policy.",
	"user_subnet.no_exit_nodes"                   : "No exit-nodes in headscale yet. Add a node with <code>tag:exit-node</code> via /admin/exit-nodes first.",
	"user_subnet.v0_16_0_note_title"              : "What v0.16.0 vs v0.16.1",
	"user_subnet.v0_16_0_note_body"               : "<b>v0.16.0 (this release):</b> schema + CIDR allocator + admin UI + bot <code>/mysubnet</code>. The <i>Allocate subnet</i> button creates a row in <code>user_subnets</code> with status <code>pending</code>.<br><br><b>v0.16.1:</b> real sidecar container + headscale preauth + node tagging + route approval. After v0.16.1 ships, the <i>Sanity check</i> will report the router node_id and <code>/mysubnet</code> will show status <code>active</code> instead of <code>pending</code>.",
	"user_subnet.use_cases_title"                 : "Why would a user want this?",
	"user_subnet.use_cases_body"                  : "A <b>subnet-router</b> is a Tailscale node on the user's side that advertises <code>10.0.&lt;uid&gt;.0/24</code> into the tailnet. After the route is approved, every tailnet client with <code>--accept-routes</code> sees the user's home network as part of the tailnet. Typical scenarios: <ul style=\"margin:4px 0 4px 18px;padding:0\"><li><b>Home NAS</b> (Plex, Synology, TrueNAS) — reachable from the phone on the road.</li><li><b>Smart home / IoT</b> (Home Assistant, MQTT, cameras) — stays on the LAN, visible only via the tailnet.</li><li><b>Office server room</b> (Proxmox, NAS, printers) — team gets access without opening firewall ports.</li><li><b>Family sharing</b> — parents' photos / videos visible to kids' tablets, without giving kids admin on the NAS.</li><li><b>Lab / dev</b> — VMs, test DBs on <code>10.0.&lt;uid&gt;.0/24</code>, reachable from the laptop anywhere.</li><li><b>Cross-site backup</b> — two physical sites with their own LANs, joined via tailnet for replication.</li></ul>If the user has only 1-2 devices, just install Tailscale on each directly. A subnet-router pays off at 5+ devices, or when some devices can't run Tailscale themselves (printer, ESP32, old NAS).",
	"user_subnet.onboard_title"                   : "How to onboard a user",
	"user_subnet.onboard_step1"                   : "<b>1. Allocate the subnet</b> (if not done yet). The <i>\"Allocate subnet\"</i> button above creates a row in <code>user_subnets</code> with status <code>pending</code> and CIDR <code>10.0.&lt;uid&gt;.0/24</code>.",
	"user_subnet.onboard_step2"                   : "<b>2. Hand the user a preauth</b>. Two options: <ul style=\"margin:4px 0 4px 18px;padding:0\"><li><i>\"Download bundle (.tar.gz)\"</i> button — gives a tar.gz with <code>setup.sh</code> + <code>README.md</code> + <code>commands.txt</code> + <code>CIDR.txt</code>. User scps to their router, untars, runs <code>sudo bash commands.txt</code>.</li><li><i>\"Issue preauth key\"</i> button — gives just the <code>tailscale up</code> command (paste into chat).</li></ul>Both issue a preauth with 1-hour TTL, single-use, tag <code>tag:subnet-router</code>.",
	"user_subnet.onboard_step3"                   : "<b>3. Wait for auto-approve</b>. Within ~30s, the sidecar sees the new node in headscale, approves the <code>10.0.&lt;uid&gt;.0/24</code> route, fills in <code>user_subnets.router_node_id</code>, and flips the status to <code>router_active</code>. The status pill here updates on the next <code>/my/devices</code> load (operator or user). Check the sidecar logs to see \"sidecar: approved 10.0.&lt;uid&gt;.0/24 on node …\"",
	"user_subnet.docs_link_text"                  : "Full guide for the user (README, use-cases, troubleshooting)",
	"user_subnet.column_header"                   : "Subnet",
	"user_subnet.cell_none"                       : "—",
	"user_subnet.cell_pending"                    : "pending",
	"user_subnet.cell_active"                     : "active",
	"user_subnet.cell_disabled"                   : "disabled",
	"user_subnet.open_button"                     : "Subnet",
	"user_subnet.cell_router_active"              : "router active",
	"user_subnet.status_pill_active_title"        : "Subnet active: user has devices in the tailnet",
	"user_subnet.status_pill_pending_title"       : "Subnet pending: user has not added any devices yet",
	"user_subnet.status_pill_router_active_title" : "Subnet-router up: 10.0.<uid>.0/24 is actually being advertised in the tailnet via tag:subnet-router",
	"user_subnet.status_pill_disabled_title"      : "Subnet disabled by an admin",
	"user_subnet.status_explainer_v0_22_3"        : "<b>v0.22.3:</b> the <code>active</code> status now means \"user has devices in the tailnet\", not \"subnet-router is up\". <code>router active</code> is a bonus status that appears when the user additionally set up a subnet-router to route their home network. <code>pending</code> is only for fresh users who haven't added any devices yet.",
}
