# etcd env (v0.32.21).
# Set SKYGATE_ETCD_HOST to the public IP of SVYATOSLAVA
# (the VM where etcd is running).
#
# On skygate-host-1, this file has:
#   SKYGATE_ETCD_HOST=198.51.100.1  # skygate-host-2's public IP
# On skygate-host-2:
#   SKYGATE_ETCD_HOST=127.0.0.1  # self

SKYGATE_ETCD_HOST=127.0.0.1
