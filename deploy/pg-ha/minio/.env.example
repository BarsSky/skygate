# MinIO credentials (v0.32.21).
# Copy to .env on the host and fill in.
# Min 8 chars for both. Use a password manager.

MINIO_ROOT_USER=skygate-admin
# openssl rand -base64 32 | tr -d '/+=' | cut -c1-32
MINIO_ROOT_PASSWORD=REPLACE_ME_WITH_RANDOM_32_CHARS
